version: 1
backend:
  phases:
    build:
      commands:
        - npm install
        - npx @aws-amplify/backend-cli generate
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'