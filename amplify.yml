version: 1
backend:
  phases:
    build:
      commands:
        - npm ci
        - npx @aws-amplify/backend-cli generate
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'